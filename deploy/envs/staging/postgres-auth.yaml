# staging overlay for helm/postgres-auth — reduced resources; persistence and security
# settings come from the chart defaults.
resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
