# prod overlay for helm/frontend — EKS: ECR image, empty host = catch-all rule,
# so the apex and www answer with prod regardless of domain. The pinned tag is
# the promotion gate: bumped by the release workflow on vX.Y.Z tags only.
image:
  repository: 809379394639.dkr.ecr.us-east-1.amazonaws.com/myfitnessrank/frontend
  tag: "0.6.0"
ingress:
  host: ""
