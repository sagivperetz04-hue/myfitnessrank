# prod overlay for helm/frontend — empty host = catch-all rule, so prod is what
# answers at http://localhost:8080. The pinned tag is the promotion gate: it is
# bumped on release, not on every master merge.
image:
  tag: "0.2.1"
ingress:
  host: ""
