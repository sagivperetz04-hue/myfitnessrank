MyFitnessRank frontend deployed.

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Image:     {{ .Values.image.repository }}:{{ .Values.image.tag }}

{{- if .Values.ingress.enabled }}

Ingress is enabled{{ if .Values.ingress.host }} for host {{ .Values.ingress.host }}{{ else }} as a catch-all rule{{ end }}.
On the local kind cluster the app is reachable at:
  http://localhost:8080
{{- end }}

Check rollout:
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "frontend.fullname" . }}

/api/ requests are proxied to: {{ .Values.backendUrl }}
