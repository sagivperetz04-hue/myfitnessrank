MyFitnessRank leaderboards PostgreSQL deployed.

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Image:     {{ .Values.image.repository }}:{{ .Values.image.tag }}

The leaderboard schema runs automatically on FIRST boot only
(initdb scripts are skipped when the data volume is already initialized).
Seeding the dataset is a separate step — the leaderboards chart's post-install Job.

Connection (in-cluster):
  host: {{ include "postgres-leaderboards.fullname" . }}.{{ .Release.Namespace }}.svc
  port: {{ .Values.service.port }}

Requires secret "{{ .Values.existingSecret }}" (keys: username, password, database)
to exist in namespace {{ .Release.Namespace }} before the pod becomes ready.

Check status:
  kubectl -n {{ .Release.Namespace }} rollout status statefulset/{{ include "postgres-leaderboards.fullname" . }}
