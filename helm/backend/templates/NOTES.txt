MyFitnessRank backend deployed.

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Image:     {{ .Values.image.repository }}:{{ .Values.image.tag }}

The API is internal-only by default — the frontend nginx proxies /api/ to it.

Check rollout:
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "backend.fullname" . }}

Smoke-test from inside the cluster:
  kubectl -n {{ .Release.Namespace }} run curl --rm -it --image=curlimages/curl --restart=Never -- \
    curl -s http://{{ include "backend.fullname" . }}:{{ .Values.service.port }}/health

Requires secret "{{ .Values.db.existingSecret }}" (keys: username, password, database)
to exist in namespace {{ .Release.Namespace }} before pods become ready.
